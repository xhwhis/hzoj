#include <iostream>
#include <cstring>
using namespace std;

int t, n, h, r, ball[1005][3], low[1005], low_num, up[1005], arr[1005][1005], check[1005];

int main() {
    cin >> t;
    while (t--) {
        memset(low, 0, sizeof(low));
        memset(up, 0, sizeof(up));
        memset(arr, 0, sizeof(arr));
        memset(check, 0, sizeof(check));
        low_num = 0;
        cin >> n >> h >> r;
        for (int i = 1; i < )
    }
    return 0;
}
