#include <iostream>
using namespace std;

struct node {
    int x, y;
};

int n, m;

int main() {
    cin >> n >> m;
    for (int i = 1; i <= n; i++) {
        cin >> &mmap[i][1];
    }
    queue<node> que;
    que.push({1, 1, 0, d});
    for (int i = 0; i < 4; i++) {
        for (int j = 2; j <= tmp.d; j++) {
            int x = 
        }
    }
    return 0;
}
